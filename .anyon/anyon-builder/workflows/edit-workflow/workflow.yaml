# Edit Workflow - Workflow Editor Configuration
name: "edit-workflow"
description: "Edit existing ANYON workflows while following all best practices and conventions"
author: "Anyon"

# Critical variables load from config_source
config_source: "{project-root}/.anyon/anyon-builder/config.yaml"
communication_language: "{config_source}:communication_language"
user_name: "{config_source}:user_name"

# Required Data Files - Critical for understanding workflow conventions
workflow_creation_guide: "{project-root}/.anyon/anyon-builder/workflows/create-workflow/workflow-creation-guide.md"
workflow_execution_engine: "{project-root}/.anyon/core/tasks/workflow.xml"

# Reference examples
workflow_examples_dir: "{project-root}/.anyon/anyon-method/workflows/"

# Module path and component files
installed_path: "{project-root}/.anyon/anyon-builder/workflows/edit-workflow"
template: false # This is an action workflow - no template needed
instructions: "{installed_path}/instructions.md"
validation: "{installed_path}/checklist.md"

standalone: true

# Web bundle configuration