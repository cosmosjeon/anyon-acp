# {{ticket_id}}: {{title}}

## 메타데이터

| 필드 | 값 |
|------|-----|
| **ID** | {{ticket_id}} |
| **Epic** | {{epic}} |
| **Type** | {{type}} |
| **Priority** | {{priority}} |
| **Wave** | {{wave}} |
| **Status** | pending |

## 설명

{{description}}

## 담당 에이전트

| 순서 | 에이전트 | 역할 |
|------|----------|------|
{{#each assigned_agents}}
| {{order}} | **{{agent}}** | {{responsibility}} |
{{/each}}

## 참조 문서

{{#each input_documents}}
- `{{document}}` > {{section}}
{{/each}}

## 의존성

```yaml
blocked_by:
{{#each blocked_by}}
  - {{ticket_id}} ({{title}})
{{/each}}
{{#if no_blocked_by}}
  # 없음 - Wave 1 티켓
{{/if}}

blocks:
{{#each blocks}}
  - {{ticket_id}} ({{title}})
{{/each}}
{{#if no_blocks}}
  # 없음 - 최종 티켓
{{/if}}

parallel_with:
{{#each parallel_with}}
  - {{ticket_id}} ({{title}})
{{/each}}
{{#if no_parallel}}
  # 없음 - 순차 실행 필요
{{/if}}
```

## TDD 개발 플로우

> ⚠️ **필수**: 모든 구현은 TDD 사이클을 따라야 합니다.

### 1. RED: 테스트 작성 (먼저!)

```
{{#each tdd_tests}}
- [ ] {{test_description}}
{{/each}}
{{#if no_tdd_tests}}
- [ ] 정상 케이스 테스트 작성
- [ ] 엣지 케이스 테스트 작성
- [ ] 에러 케이스 테스트 작성
{{/if}}
```

**테스트 파일 위치**: `{{test_file_path}}`

### 2. GREEN: 최소 구현

- [ ] 테스트를 통과하는 최소한의 코드 작성
- [ ] 모든 테스트 통과 확인

### 3. REFACTOR: 코드 개선

- [ ] 중복 제거
- [ ] 네이밍 개선
- [ ] 코드 구조 정리
- [ ] 테스트 여전히 통과 확인

---

## 수용 기준

{{#each acceptance_criteria}}
- [ ] {{criterion}}
{{/each}}

### 테스트 관련 수용 기준

- [ ] 단위 테스트 작성 완료
- [ ] 테스트 커버리지 80% 이상
- [ ] 모든 테스트 통과
- [ ] 엣지 케이스 테스트 포함

## 산출물

### 테스트 파일
```
{{test_file_path}}
```

### 구현 파일
```
{{#each output_artifacts}}
{{artifact}}
{{/each}}
```

## 기술 명세

{{#if technical_spec}}
```{{language}}
{{technical_spec}}
```
{{else}}
_담당 에이전트가 구현 시 작성_
{{/if}}

## 실행 로그

```
[pending] 대기 중{{#if blocked_by}} - {{blocked_by_summary}} 완료 필요{{/if}}
```

---

_Generated by PM Orchestrator on {{date}}_
