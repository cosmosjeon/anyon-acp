# 템플릿 기능 기획서

## 개요

프로젝트 선택 후 템플릿을 선택하는 단계를 추가하여, 프로젝트 유형에 따라 다른 워크스페이스를 제공하는 기능.

---

## 변경되는 유저 플로우

### 기존 플로우
```
ProjectList → WorkspaceSelector (MVP/Maintenance) → Workspace
```

### 새로운 플로우
```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              유저 플로우 다이어그램                                │
└─────────────────────────────────────────────────────────────────────────────────┘

                              ┌─────────────────┐
                              │   ProjectList   │
                              │  (프로젝트 목록)  │
                              └────────┬────────┘
                                       │
                                       │ 폴더 클릭
                                       ▼
                              ┌─────────────────┐
                              │  템플릿 설정됨?   │
                              └────────┬────────┘
                                       │
                       ┌───────────────┴───────────────┐
                       │                               │
                       ▼ NO                            ▼ YES
              ┌─────────────────┐             ┌─────────────────┐
              │TemplateSelector │             │  Basic 템플릿    │
              │  (템플릿 선택)   │             └────────┬────────┘
              └────────┬────────┘                      │
                       │                               │
         ┌─────────────┼─────────────┐                │
         │             │             │                │
         ▼ Basic       ▼ 다른 템플릿  │                │
┌─────────────┐  ┌───────────┐      │                │
│ 템플릿 저장  │  │Toast 표시 │      │                │
│ → 다음 단계  │  │"출시 예정" │      │                │
└──────┬──────┘  └───────────┘      │                │
       │                            │                │
       └────────────────────────────┼────────────────┘
                                    │
                                    ▼
                           ┌─────────────────┐
                           │WorkspaceSelector│
                           │ (MVP/Maintenance)│
                           └────────┬────────┘
                                    │
                        ┌───────────┴───────────┐
                        │                       │
                        ▼ MVP                   ▼ Maintenance
                ┌─────────────────┐     ┌─────────────────┐
                │  MvpWorkspace   │     │MaintenanceWork- │
                │  (MVP 개발)     │     │    space        │
                └─────────────────┘     └─────────────────┘
```

**핵심 로직:**
- 템플릿이 **아직 설정 안 된 프로젝트** → `TemplateSelector`로 이동
- 템플릿이 **이미 설정된 프로젝트** → 해당 템플릿의 워크스페이스로 바로 이동
- **Basic 템플릿** → 기존 `WorkspaceSelector` (MVP/Maintenance)
- **다른 템플릿** → "출시 예정" Toast 표시 (선택 불가)
- **기존 프로젝트** → 자동으로 Basic 템플릿 적용

---

## 템플릿 종류 (더미)

| 템플릿 ID | 이름 | 아이콘 | 상태 | 설명 |
|-----------|------|--------|------|------|
| `basic` | Basic | 📦 | **사용 가능** | 웹/앱 개발 (MVP & 유지보수) |
| `ai-agent` | AI Agent | 🤖 | 출시 예정 | AI 에이전트 개발 |
| `api-server` | API Server | 🔌 | 출시 예정 | 백엔드 API 서버 |
| `data-pipeline` | Data Pipeline | 📊 | 출시 예정 | 데이터 파이프라인 |
| `mobile-app` | Mobile App | 📱 | 출시 예정 | 모바일 앱 개발 |

---

## 데이터 저장

### 저장 위치
앱 설정 (`api.saveSetting`)에 프로젝트별 템플릿 매핑 저장

> 기존 `registered_projects` 저장 방식과 동일하게 앱 설정에 저장

### 저장 구조
```json
// Setting key: "project_templates"
{
  "/Users/cosmos/my-project": "basic",
  "/Users/cosmos/ai-agent-project": "ai-agent"
}
```

### API 추가 (api.ts)
```typescript
// 템플릿 매핑 전체 조회
getProjectTemplates(): Promise<Record<string, string>>

// 특정 프로젝트 템플릿 조회
getProjectTemplate(projectPath: string): Promise<string | null>

// 템플릿 저장
setProjectTemplate(projectPath: string, templateId: string): Promise<void>
```

---

## UI 컴포넌트

### 1. TemplateSelector (신규)

**경로:** `src/components/TemplateSelector.tsx`

**레이아웃:**
```
┌────────┬─────────────────────────────────────────┐
│Sidebar │       Template Selection Grid           │
│ (56px) │                                         │
│        │  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│        │  │  Basic  │  │AI Agent │  │API Srvr │ │
│        │  │   ✓     │  │ 출시예정 │  │ 출시예정 │ │
│        │  └─────────┘  └─────────┘  └─────────┘ │
│        │                                         │
│        │  ┌─────────┐  ┌─────────┐              │
│        │  │  Data   │  │ Mobile  │              │
│        │  │ 출시예정 │  │ 출시예정 │              │
│        │  └─────────┘  └─────────┘              │
└────────┴─────────────────────────────────────────┘
```

**동작:**
- Basic 클릭 → 템플릿 저장 → WorkspaceSelector로 이동
- 다른 템플릿 클릭 → Toast: "출시 예정입니다"

---

### 2. ProjectCard 수정

**변경 사항:** 템플릿 태그 뱃지 추가

**디자인:**
```
  템플릿 미설정 (태그 없음)             템플릿 설정됨
  ┌──────────────────────┐            ┌──────────────────────┐
  │ 📁                 ⋮ │            │ 📁                 ⋮ │
  │                      │            │                      │
  │ my-project           │            │ my-project           │
  │ ~/Documents/...      │            │ ~/Documents/...      │
  │ 🕐 2h ago            │            │ 🕐 2h ago   [Basic]  │ ← 태그
  └──────────────────────┘            └──────────────────────┘
```

**태그 스타일:**
- 크기: `text-xs`
- 배경: `bg-primary/10`
- 텍스트: `text-primary`
- 패딩: `px-2 py-0.5`
- 모서리: `rounded-full`

**태그 표시 규칙:**
- 템플릿 미설정: 태그 표시 안 함
- Basic 템플릿: `[Basic]`
- 다른 템플릿: 해당 템플릿 이름 (예: `[AI Agent]`)

---

### 3. ProjectRoutes 수정

**라우트 타입 추가:**
```typescript
type ProjectsRouteType =
  | { type: 'list' }
  | { type: 'template-selector'; projectId: string }  // 신규
  | { type: 'workspace-selector'; projectId: string }
  | { type: 'mvp'; projectId: string }
  | { type: 'maintenance'; projectId: string };
```

**네비게이션 함수 추가:**
```typescript
goToTemplateSelector: (projectId: string) => void
```

**goToProject 로직 변경:**
```typescript
const goToProject = async (projectId: string) => {
  const project = getProjectById(projectId);
  const template = await api.getProjectTemplate(project.path);

  if (!template) {
    // 템플릿 미설정 → 템플릿 선택 화면
    navigate({ type: 'template-selector', projectId });
  } else if (template === 'basic') {
    // Basic → 워크스페이스 선택
    navigate({ type: 'workspace-selector', projectId });
  } else {
    // 다른 템플릿 → 해당 워크스페이스 (추후 구현)
    navigate({ type: 'workspace-selector', projectId });
  }
};
```

---

## 파일 변경 목록

| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `src/components/TemplateSelector.tsx` | **신규** | 템플릿 선택 화면 |
| `src/components/ProjectRoutes.tsx` | 수정 | 라우트 타입 및 네비게이션 로직 |
| `src/components/ProjectCard.tsx` | 수정 | 템플릿 태그 뱃지 |
| `src/components/ProjectListView.tsx` | 수정 | 템플릿 정보 로드 |
| `src/lib/api.ts` | 수정 | 템플릿 API 함수 추가 |
| `src/types/template.ts` | **신규** | 템플릿 타입 정의 |

> **참고:** Tauri 백엔드 수정 불필요 - 기존 `saveSetting`/`getSetting` API 활용

---

## 타입 정의

```typescript
// src/types/template.ts

export type TemplateId = 'basic' | 'ai-agent' | 'api-server' | 'data-pipeline' | 'mobile-app';

export interface Template {
  id: TemplateId;
  name: string;
  description: string;
  icon: string;  // emoji 또는 아이콘 컴포넌트명
  available: boolean;
}

export const TEMPLATES: Template[] = [
  {
    id: 'basic',
    name: 'Basic',
    description: '웹/앱 개발을 위한 기본 템플릿 (MVP & 유지보수)',
    icon: '📦',
    available: true,
  },
  {
    id: 'ai-agent',
    name: 'AI Agent',
    description: 'AI 에이전트 개발 및 배포',
    icon: '🤖',
    available: false,
  },
  {
    id: 'api-server',
    name: 'API Server',
    description: '백엔드 API 서버 개발',
    icon: '🔌',
    available: false,
  },
  {
    id: 'data-pipeline',
    name: 'Data Pipeline',
    description: '데이터 수집 및 처리 파이프라인',
    icon: '📊',
    available: false,
  },
  {
    id: 'mobile-app',
    name: 'Mobile App',
    description: '모바일 앱 개발 (iOS/Android)',
    icon: '📱',
    available: false,
  },
];
```

---

## 에러/로딩 처리

| 상황 | 처리 |
|------|------|
| 템플릿 설정 읽기 실패 | Basic으로 간주 |
| 템플릿 저장 실패 | Toast 에러 표시, 재시도 가능 |
| ProjectCard 로딩 중 | 태그 표시 안 함 (로딩 인디케이터 없음) |

---

## 구현 순서

1. **타입 정의** - `src/types/template.ts`
2. **API 함수** - `src/lib/api.ts`
3. **TemplateSelector 컴포넌트** - 신규 생성
4. **ProjectRoutes 수정** - 라우트 및 네비게이션 로직
5. **ProjectCard 수정** - 템플릿 태그 표시
6. **테스트** - 전체 플로우 확인

---

## 향후 확장 계획

- 각 템플릿별 전용 워크스페이스 구현
- 템플릿 변경 기능 (설정에서)
- 커스텀 템플릿 생성 기능
