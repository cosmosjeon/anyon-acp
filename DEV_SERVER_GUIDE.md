# 개발 서버 가이드

이 문서는 opcode 프로젝트의 개발 서버 관련 문제를 해결하고 올바르게 사용하는 방법을 설명합니다.

## 🔧 문제 해결 완료

### 1. 캐싱 문제
- ✅ Vite 강제 캐시 클리어 설정
- ✅ HMR (Hot Module Replacement) 최적화
- ✅ React Fast Refresh 활성화
- ✅ 파일 감시 100ms 간격으로 설정

### 2. 프로세스 중복 실행 문제
- ✅ Tauri 설정에 `beforeDevCommand` 추가
- ✅ 개발 서버 자동 시작/종료 관리
- ✅ 프로세스 강제 종료 스크립트 추가

## 🚀 사용 방법

### ⚡ 일반 개발 (권장)

**NPM 사용:**
```bash
npm run tauri dev
```

**Bun 사용 (더 빠름):**
```bash
npm run tauri:bun
# 또는 직접
bun run tauri:bun
```

이제 Tauri가 자동으로 Vite 서버를 시작하고 관리합니다!

### 🧹 프로세스가 꼬였을 때

여러 개의 서버가 중복 실행되거나 문제가 생겼을 때:

```bash
# 모든 개발 서버 강제 종료
npm run kill:servers

# 그 다음 다시 시작
npm run tauri dev
```

### 🔄 캐시 문제 발생 시

코드 변경이 반영되지 않을 때:

```bash
# 1. 캐시 삭제
npm run clean:cache

# 2. 개발 서버 시작
npm run tauri dev
```

또는 한 번에:
```bash
npm run dev:clean
```

## 📋 사용 가능한 명령어

### NPM 명령어

| 명령어 | 설명 |
|--------|------|
| `npm run tauri dev` | Tauri + Vite 개발 서버 시작 (NPM) |
| `npm run tauri:bun` | Tauri + Vite 개발 서버 시작 (Bun, 더 빠름) |
| `npm run dev` | Vite 개발 서버만 시작 |
| `npm run dev:clean` | 캐시 삭제 후 Vite 서버 시작 |
| `npm run kill:servers` | 모든 개발 서버 강제 종료 |
| `npm run clean:cache` | 캐시 디렉토리 삭제 |
| `npm run build` | 프로덕션 빌드 |

### Bun 명령어 (더 빠름 ⚡)

| 명령어 | 설명 |
|--------|------|
| `bun run tauri:bun` | Tauri + Vite 개발 서버 시작 (Bun) |
| `bun run dev:bun` | Vite 개발 서버만 시작 (Bun) |
| `bun run dev:bun:clean` | 캐시 삭제 후 Vite 서버 시작 (Bun) |
| `bun run kill:servers` | 모든 개발 서버 강제 종료 |
| `bun run clean:cache` | 캐시 디렉토리 삭제 |
| `bun run build:bun` | 프로덕션 빌드 (Bun) |

## ⚠️ 주의사항

### ❌ 하지 말아야 할 것

```bash
# 이렇게 하면 프로세스가 중복됩니다!
npm run dev &          # Vite 서버 시작
npm run tauri dev      # Tauri가 또 Vite를 시작하려고 함
```

### ✅ 올바른 방법

```bash
# NPM 사용
npm run tauri dev

# 또는 Bun 사용 (더 빠름)
bun run tauri:bun
```

## 🔍 문제 진단

### 여러 서버가 실행 중인지 확인

```bash
ps aux | grep -E "(tauri|vite)" | grep -v grep
```

### 포트가 사용 중인지 확인

```bash
lsof -ti:1420,1421
```

### 수동으로 프로세스 종료

```bash
# 특정 PID 종료
kill -9 <PID>

# 또는 스크립트 사용
npm run kill:servers
```

## 🎯 실시간 반영 확인

코드를 변경하고 저장하면:

1. **100ms 이내** - Vite가 변경사항 감지
2. **즉시** - React Fast Refresh 실행
3. **0.5초 이내** - 화면에 반영

상태가 유지되면서 컴포넌트만 교체됩니다!

## 💡 팁

### 🔥 Bun을 사용하면 더 빠릅니다!

Bun은 NPM보다 3-5배 빠른 패키지 매니저입니다:

```bash
# Bun 설치 (아직 없다면)
curl -fsSL https://bun.sh/install | bash

# Bun으로 개발 서버 시작
bun run tauri:bun

# 다른 명령어들
bun run kill:servers
bun run clean:cache
bun run dev:bun:clean
```

**차이점:**
- `npm run tauri dev` → NPM으로 Vite 실행
- `bun run tauri:bun` → Bun으로 Vite 실행 (더 빠름)

### 완전 초기화

모든 것을 초기화하고 싶을 때:

```bash
npm run kill:servers
npm run clean:cache
rm -rf node_modules
npm install
npm run tauri dev
```

## 📞 여전히 문제가 있나요?

1. 모든 서버 종료: `npm run kill:servers`
2. 캐시 삭제: `npm run clean:cache`
3. 터미널 재시작
4. `npm run tauri dev` 다시 실행

그래도 안 되면 이슈를 보고해주세요!
