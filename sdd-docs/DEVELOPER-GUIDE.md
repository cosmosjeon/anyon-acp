# ANYON 개발자 가이드

> 코드 품질과 문서를 자동으로 관리하는 SDD (Spec-Driven Development) 시스템 사용법

---

## 한눈에 보기

```
┌─────────────────────────────────────────────────────────────┐
│  코드 작성 → commit → push → 자동 검사 → 문서 동기화       │
│                                                             │
│  🔴 Critical 이슈 → push 차단 → 수정 후 재시도             │
│  ✅ 통과 → 자동으로 문서 업데이트 → push 완료              │
└─────────────────────────────────────────────────────────────┘
```

---

## 초기 설정 (처음 한 번만)

프로젝트 클론 후 husky를 설치해야 pre-push hook이 작동합니다:

```bash
npm install
npx husky install
```

---

## Push할 때 자동으로 일어나는 일

코드를 push하면 자동으로:

1. **코드 품질 검사** - Critical 이슈 있으면 push 차단
2. **문서 동기화** - 변경된 코드에 맞춰 문서 업데이트
3. **Push 진행** - 문서 변경도 함께 커밋

```bash
git push origin dev

# 출력 예시:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 코드 검사 중...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

검사 대상 파일:
  📁 src/components/NewFeature.tsx
  📁 src/hooks/useData.ts

...

✅ 코드 검사 통과!
📝 문서 동기화 중...
✅ push를 진행합니다.
```

---

## Push가 차단됐을 때

### 터미널에서

```bash
🔴 Push가 차단되었어요
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

지금 바로 수정할까요? (y/n): y

🔧 수정 중...
✅ 수정 완료!

변경사항을 확인하고 다시 commit & push 해주세요:

   git add .
   git commit --amend --no-edit
   git push
```

### GUI (VS Code, Cursor 등)에서

터미널을 열고:

```bash
claude "발견된 코드 품질 이슈를 수정해줘"
```

---

## 슬래시 커맨드

Claude Code에서 사용할 수 있는 명령어들:

### `/sdd:code-audit` - 전체 코드 감사

```bash
/sdd:code-audit
```

- Frontend, Desktop, Server 전체 분석
- 결과: `sdd-docs/audits/code-audit-report.md`

### `/sdd:code-refactor` - 자동 리팩토링

```bash
# 먼저 감사 실행
/sdd:code-audit

# 발견된 이슈 자동 수정
/sdd:code-refactor
```

- 우선순위 선택 (P0/P1/P2)
- 자동 수정 가능한 이슈만 처리
- 결과: `sdd-docs/audits/refactor-report.md`

### `/sdd:sync-docs` - 문서 동기화

```bash
/sdd:sync-docs
```

- 코드 변경에 맞춰 문서 업데이트
- 결과: `sdd-docs/specs/*.md` 업데이트

### `/sdd:audit-codebase` - 상세 코드 감사

```bash
/sdd:audit-codebase
```

- 보안, 복잡도, 아키텍처 심층 분석
- `/sdd:code-audit`보다 더 상세한 분석

---

## 검사 기준

### Critical (Push 차단)

- 50줄 이상 함수
- 심각한 중복 코드
- 보안 취약점 (하드코딩된 시크릿 등)

### Warning (권장 수정)

- 30-50줄 함수
- any 타입 사용 (TypeScript)
- unwrap() 과다 사용 (Rust)

### Info (참고)

- TODO 주석
- 네이밍 개선 가능

---

## 스킵하고 싶을 때

### 코드 검사 스킵

```bash
SKIP_AUDIT=1 git push
```

### 문서 동기화만 스킵

```bash
SKIP_SYNC=1 git push
```

**주의**: 긴급 상황에서만 사용하세요!

---

## 폴더 구조

```
sdd-docs/
├── CURRENT-STATUS.md      # 현재 프로젝트 상태 (세션 시작시 자동 로드)
├── DEVELOPER-GUIDE.md     # 👈 이 문서
├── index.md               # 문서 마스터 인덱스
│
├── specs/                 # 기술 스펙 (자동 동기화)
│   ├── project-overview.md
│   ├── architecture-*.md
│   └── component-inventory.md
│
└── audits/                # 코드 감사 결과
    ├── code-audit-report.md    # 전체 감사 보고서
    ├── audit-result.json       # JSON 형식 결과
    ├── refactor-report.md      # 리팩토링 결과
    └── frontend/desktop/server # 영역별 상세
```

---

## 빠른 참조

| 하고 싶은 일 | 명령어/방법 |
|-------------|-------------|
| 초기 설정 | `npm install && npx husky install` |
| 코드 push | `git push` (자동 검사됨) |
| 전체 코드 감사 | `/sdd:code-audit` |
| 상세 코드 감사 | `/sdd:audit-codebase` |
| 이슈 자동 수정 | `/sdd:code-refactor` |
| 문서 동기화 | `/sdd:sync-docs` |
| 검사 스킵 | `SKIP_AUDIT=1 git push` |
| 감사 결과 확인 | `sdd-docs/audits/code-audit-report.md` |

---

## FAQ

### Q: Push할 때마다 검사하면 느리지 않나요?

변경된 파일만 검사합니다. 대부분 10-30초 내에 완료됩니다.

### Q: 자동 수정이 잘못되면 어떡하나요?

`/sdd:code-refactor`는 수정 전에 `git stash`로 롤백 포인트를 만듭니다:

```bash
# 문제가 생기면
git stash pop
```

### Q: Claude Code가 없으면?

Pre-push 검사가 자동으로 스킵됩니다. 수동으로 `/sdd:code-audit` 실행 후 push하세요.

### Q: Hook이 작동 안 해요

husky가 설치되었는지 확인하세요:

```bash
npx husky install
```

---

**문서 업데이트**: 2025-12-20
